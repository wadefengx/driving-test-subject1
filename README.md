# C1 驾照 · 科目一（智能刷题 + 模拟考试 · PWA）

一个零依赖、可离线、可安装的科目一考试 PWA 网页。支持 **SM-2 间隔重复智能刷题**、**仿真模拟考试**、**错题重练**三种模式，CDN 图片自动缓存本地，手机安装后可完全替代驾考 App 做日常练习。

## 🚀 在线使用

访问 **[https://agentmystia.github.io/driving-test-subject1/](https://agentmystia.github.io/driving-test-subject1/)**（GitHub Pages 自动部署）

**安装到手机桌面**（推荐，体验等同原生 App）：
- **iOS Safari**：打开网址 → 分享 → 「添加到主屏幕」
- **Android Chrome**：打开网址 → 菜单 → 「添加到主屏幕」/「安装应用」
- **桌面 Chrome/Edge**：地址栏右侧安装图标 → 安装

安装后全屏启动、无浏览器地址栏，离线可用（首次需联网加载）。

## 三种模式

### 📚 刷题练习（智能精选）
基于 **SM-2 间隔重复算法**抽题，用最少时间巩固最多知识点：
- 优先复习**今日到期**的题目（错过的题间隔到期需要巩固）
- 引入适量**新题**（控制每日新题量，避免过载）
- 答题**即时判定**：答对变绿、答错所选变红并标出正确答案
- 立即展开**正确答案 / 解析 / 法条依据**
- 不限时，节奏完全可控

### 📝 模拟考试（仿真）
对标真实上机考试：
- 判断题 **40** + 单选题 **60**，章节分层抽样
- 限时 **45 分钟**，到点自动交卷
- 答题不揭示对错，交卷后统一评定
- 分数环 + 对错统计 + 逐题解析（错题置顶、可筛选）
- 答错的题自动注入 SM-2 复习队列

### ❌ 错题重练
只练**错题本**中的题，与 SM-2 复习队列完全分离：
- 任何模式答错自动收录（含模考未答题，未答按答错处理，与真实考试一致）
- 任意模式答对一次即移出错题本
- 出题时 SM-2 今日到期的错题优先，答错重置间隔（明天再来）
- 全部攻克后首页错题数清零

## 🧠 SM-2 间隔重复算法原理

每题记录难度系数（ease）与复习间隔（interval）：

| 行为 | 算法响应 |
|------|---------|
| 答对 | interval 增长：1天 → 3天 → 7天 → 16天 → 35天…（按 ease 倍增），ease 微增 |
| 答错 | interval 重置为 1 天，ease 降低（最低 1.3） |

效果：**已掌握的题间隔越来越长、少打扰；薄弱的题频繁出现、强化记忆**。科学依据艾宾浩斯遗忘曲线，避免重复刷已会的题，专注薄弱点。

掌握判定：连续答对 ≥3 次且间隔 ≥7 天 → 标记为"已掌握"。

## 🖼️ 图片离线缓存

题目图片来自 CDN，采用 **Service Worker + Cache API** 实现本地缓存：

- **懒缓存**：看一题自动缓存一题，下次离线也能看
- **一键预下载**：首页点「📥 预下载图片库」可批量下载全部 714 张图片（约 50-100MB），完成后完全离线可用
- 预下载带进度条，可随时取消

## 📊 学习数据持久化

所有数据存储在浏览器本地（localStorage + Cache API），不上传任何服务器：
- SM-2 间隔记录（每题的 ease/interval/reps/due）
- 错题本（答错自动收录，答对一次即移出）
- 答题统计（累计正确率、模考历史、连续学习天数）
- 图片缓存（Cache API）

首页可「↺ 重置进度」清空学习记录重新开始。

## 本地开发与构建

```bash
python3 scripts/fetch_bank.py    # 下载/刷新原始题库（可选，data/ 已含）
python3 scripts/classify.py      # 规范化与分类（可选）
python3 scripts/build_html.py    # 生成 index.html / manifest.json / sw.js / 图标
```

依赖：Python 3.6+ + Pillow（生成图标用，`pip install Pillow`）。

**本地预览**：构建后双击 `index.html` 即可（功能正常，但 Service Worker 仅在 http/https 下生效）。
完整 PWA 体验需起本地服务：`python3 -m http.server` 后访问 `http://localhost:8000`。

## 文件结构

```
科目一/
├── src/                      ★ 源码（纳入版本管理）
│   ├── index.template.html   页面骨架（含构建占位符）
│   ├── style.css             样式
│   ├── app.js                应用逻辑（三模式 + SM-2 + 错题本）
│   └── sw.template.js        Service Worker
├── scripts/
│   ├── fetch_bank.py         下载原始题库
│   ├── classify.py           规范化+去重+章节分类+时效修订
│   └── build_html.py         ★ 构建：读取 src/ 内联打包，输出 index.html/manifest/sw.js/图标
├── data/
│   └── bank.json             标准题库（2115题，数据源）
├── .github/workflows/
│   └── deploy.yml            GitHub Actions 自动构建+部署 Pages
└── .gitignore
```

构建产物（index.html / manifest.json / sw.js / icon-*.png）由 `.gitignore` 忽略，由 CI 自动重建。

## 数据来源与权威性

- **原始数据**：开源项目 [`doupoa/DrivingTestSubjectOne`](https://github.com/doupoa/DrivingTestSubjectOne)，源自 **banban驾道**（最后更新 2022/07/17）
- **对应标准**：公安部 **GA/T 1575**《机动车驾驶人考试内容和方法》
- **题库规模**：科目一全国通用题 **2115 题**（已剔除地方题与重复题），含图题 714 道，解析覆盖率 99.4%

## ⚠️ 时效性说明

本题库主体对应 **2022 年 7 月** 公安部题库。已核对后续交规变化：

| 法规 | 状态 |
|------|------|
| 2022.4 记分管理办法（公安部令 163 号） | ✅ 解析已采用新记分口径 |
| 2025.1 驾驶证规定修正（公安部令 172 号） | ✅ 已修订年龄相关题目（60→63周岁、C6 为 20-70 周岁） |
| 新能源汽车、2024 便民措施等 2025 新增考点 | ⚠️ 未收录，建议考前用驾考 App 补充 |

**建议**：以本网页做日常练习与自测，临考前 1-2 周用最新版驾考宝典 / 驾校一点通 App 刷一遍最新题库，重点补新能源与新增考点。

## 局限性与免责声明

1. 题库时效性以 2022.7 为基准，**不保证 100% 与最新考试一致**。
2. 章节分类基于关键词启发式，个别题目归类可能不精确，但不影响组卷与判分。
3. 本项目仅供学习备考使用，请以最新交规与官方题库为准。考试请遵守考场纪律。
4. 原始题库版权归 banban驾道及相关权利人所有，本项目遵循其"仅供学习"的使用声明。

---

数据源：[doupoa/DrivingTestSubjectOne](https://github.com/doupoa/DrivingTestSubjectOne) · 法规依据：[公安部令第172号](https://www.mps.gov.cn:9080/n6557558/c9900238/content.html) · [记分管理办法第163号](https://www.mps.gov.cn/n6557558/c8281723/content.html)
